'use strict';

const current = require('./current');
const forecast = require('./forecast');
const save_weather = require('./save_weather');

module.exports = {
    current,
    forecast,
    save_weather
}